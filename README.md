# generator-gitlab
> Yeoman generator for gitlab.

## installation
```shell
git clone git@github.com:afeiship/generator-gitlab.git
cd generator-gitlab
npm i && npm link
```

## usage
```shell
yo @jswork/gitlab
```
